import 'package:flutter/material.dart';

class UserHaveTripWidget extends StatelessWidget {
  const UserHaveTripWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(),
      child: Column(
        children: [],
      ),
    );
  }
}
